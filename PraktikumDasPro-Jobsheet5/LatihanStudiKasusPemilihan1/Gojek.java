package LatihanStudiKasusPemilihan1;
//BELUM SELESAI
import java.util.Scanner;
import java.time.format.*;
public class Gojek {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        double jarak, tarifTotal, tarifDasar = 10000;
        String stringWaktu;
        double waktu = Double.parseDouble(stringWaktu,re)

        System.out.print("Masukkan jarak ");
        jarak = sc.nextDouble();
        System.out.print("Masukkan waktu dalam format HH.MM (Misal 07.00) ");
        stringWaktu = sc.nextLine();

        switch (07) {
            case value:
                
                break;
        
            default:
                break;
        }
    }
}
